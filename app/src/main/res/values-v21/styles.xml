<resources>

    <style name="DownloadButton" parent="AppTheme.Base">
        <item name="android:colorButtonNormal">@color/download_normal</item>
        <item name="android:colorControlHighlight">@color/download_pressed</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="TrailerButton" parent="AppTheme.Base">
        <item name="android:colorButtonNormal">@color/trailer_normal</item>
        <item name="android:colorControlHighlight">@color/trailer_pressed</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
